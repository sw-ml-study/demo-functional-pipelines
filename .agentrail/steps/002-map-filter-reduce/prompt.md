Using the selected adjacent sw-MLPL development binary, first inspect shipped syntax and tests for mapping, predicate selection, reduction, and composition. Add a focused self-checking map/filter/reduce pipeline covering empty, singleton, boundary, and duplicate inputs; add its runner, catalog row, documentation, and full just check coverage. If a semantic is unavailable, minimize it into an executable reproducer and update docs/upstream-contract.md rather than emulating misleading behavior. Run just check, commit with detailed checks, complete the step, and identify the next unblocked feature or precise upstream blocker.