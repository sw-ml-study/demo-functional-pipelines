Add an educational executable comparison showing the same transformation expressed as direct nesting, a named pipeline with explicit stages, and shipped immediate composition forms such as atop/over where semantically appropriate. Prove output equivalence over empty, singleton, boundary, and duplicate cases; explain evaluation order, readability tradeoffs, eager allocation/copy behavior, and what is not equivalent to Ramda-style reusable pipe values. Reuse existing source functions rather than duplicating them. Register the demo, runner, catalog, just recipe, and scripts/check. Update docs/upstream-contract.md with a minimized executable blocker only if reusable variadic composition values are required and unavailable. Run focused checks and just check, commit with details, complete the step, and identify the release-audit work or blocker.