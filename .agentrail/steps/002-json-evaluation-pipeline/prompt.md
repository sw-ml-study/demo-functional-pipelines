Add a fallible JSON entry point around the shipped binary metrics core. Parse records containing logits, labels, and threshold with parse_json and record_get Result composition; preserve structured missing-field errors and convert incompatible field value kinds into a stable domain error. Cover a valid realistic batch, empty batch, exact boundary, malformed JSON, each missing required field, mismatched lengths, non-binary labels, invalid threshold, and wrong field types. Reuse the numeric core, add a standalone self-checking demo and documentation updates, register catalog/just/check execution, run focused and full gates, commit with details, complete the step, and queue the final application audit.