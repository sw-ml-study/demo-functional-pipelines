Implement the pure numeric core of a realistic binary-classifier evaluation application. Compose reusable stages for sigmoid probabilities, inclusive threshold predictions, confusion-matrix counts, and safe accuracy/precision/recall metrics. Validate numeric-domain assumptions that belong in the core without JSON parsing. Cover empty, singleton positive/negative, exact-threshold, duplicates, all-positive predictions, all-negative predictions, and zero-denominator metrics. Keep the demo deterministic and self-checking, document eager/copy costs and representation invariants, register it in catalog/just/scripts/check, run focused and full checks, commit with details, complete the step, and queue JSON ingestion as the next unblocked feature.