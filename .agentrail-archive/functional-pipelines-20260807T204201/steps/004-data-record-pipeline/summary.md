Added a fallible JSON record pipeline that composes parse_json with known-schema validation and the quality partition flow. Parse errors remain Results; try/catch demotes missing and invalid field access into stable validation errors. Covered empty, boundary, duplicate, malformed, missing-field, and wrong-type inputs. Added an executable safe-record-membership upstream reproducer and gate. Commit 8379c17; focused blocker and full checks passed.