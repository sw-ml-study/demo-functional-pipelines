Build a realistic record-oriented pipeline using shipped parse_json, record access, callable/Result composition, and numeric or string-list transformations. Keep parsing and validation fallible and pure after input acquisition. Cover empty, singleton, boundary, duplicate, malformed JSON, and missing/invalid field behavior where the runtime can represent it. Document record/array representation and copy costs; register the demo, runner, catalog, just recipe, and scripts/check. If safe field validation or heterogeneous collection semantics are unavailable, produce the smallest executable reproducer, record the precise sw-mlpl feature request in docs/upstream-contract.md, and deliver the strongest honest unblocked slice. Run focused checks and just check, commit with details, complete the step, then identify the next unblocked step or blocker.