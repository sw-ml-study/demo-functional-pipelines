Inspect shipped callable, partial-application, atop/over, record, and Result semantics for a readable branching/converging pipeline. Add one realistic deterministic demo that reuses named predicates or transformations across at least two branches and combines their results without mutation. Cover empty, singleton, boundary, duplicate, and invalid/fallible input where applicable; document eager/copy behavior and any explicit loops; register it in catalog, justfile, and scripts/check. Minimize unavailable semantics into docs/upstream-contract.md with an executable reproducer. Run focused checks and just check, commit with details, complete the step, then identify the next unblocked feature or blocker.